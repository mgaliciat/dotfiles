# macOS noise
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.fseventsd
.TemporaryItems
.DocumentRevisions-V100
.com.apple.timemachine.donotpresent

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.iml
.history/
*.sublime-workspace

# Compiled / build
*.pyc
__pycache__/
.pytest_cache/
node_modules/
.venv/
venv/
dist/
build/

# Python caches
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/
*.egg-info/

# Node/JS caches
.next/
.turbo/
.parcel-cache/
*.tsbuildinfo
coverage/

# Env files (must go in the individual repo's .gitignore too)
.env
.env.local
.env.*.local

# Logs / OS
*.log
.Trashes

# Claude Code
.claude/
# per-project personal context (kickoff md) — mine only
_brief/
.covenant/
openspec/
.github/prompts/
.github/skills/
CLAUDE.local.md

# AI / agentic tools
.cursor/
.aider*
.windsurf/
.continue/

# Search index caches — rebuilt from the tree, machine-local, and big
# (8 MB on a 594-file repo). tgrep drops one in every directory it indexes.
.tgrep/

# Local specs / research (not pushed to the repo)
docs/specs/
docs/research/
planners/

# WireMock. Note this ignores the WHOLE directory, stubs included — a project
# that versions its `mappings/` + `__files/` as the contract needs a
# `!wiremock/` negation in its own .gitignore, which wins over this file.
wiremock/
.wiremock/
